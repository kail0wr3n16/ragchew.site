development:
  url: <%= ENV.fetch('DATABASE_URL').sub(/^mysql:/, 'mysql2:') %>

test:
  url: <%= ENV['TEST_DATABASE_URL'].to_s.sub(/^mysql:/, 'mysql2:') %>

production:
  url: <%= ENV.fetch('DATABASE_URL').sub(/^mysql:/, 'mysql2:') %>
