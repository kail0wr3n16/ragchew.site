development:
  url: <%= ENV['DATABASE_URL'].to_s.sub(/^mysql:/, 'mysql2:') %>

test:
  url: <%= ENV['DATABASE_URL'].to_s.sub(/^mysql:/, 'mysql2:') %>

production:
  url: <%= ENV['DATABASE_URL'].to_s.sub(/^mysql:/, 'mysql2:') %>
